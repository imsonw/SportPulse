// Expo Router handles root component registration automatically.
// The entry point must point to expo-router/entry so it can discover
// the app/ directory and build the route table at startup.
import 'expo-router/entry';
